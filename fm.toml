# Flow Manager configuration — generated by `fm init --discover`
# Detected git remote: git@github.com:mdnmdn/flow-manager.git

[provider]
type = "github"

[provider.github]
# token = "YOUR_TOKEN_HERE"
owner = "mdnmdn"
repo  = "flow-manager"

[fm]
merge_strategy = "squash"
default_target = "main"
default_wi_type = "User Story"
submodules = []
